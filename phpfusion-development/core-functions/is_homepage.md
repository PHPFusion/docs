# is_homepage()

Check if current page is set as homepage.

---

is_homepage( void ) : bool

## Parameters

No parameters.

## Return Values

(bool)

## Examples

```php
if (is_homepage()) {
    // content visible only on homepage
}
```
