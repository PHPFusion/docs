# mimetypes()

Array of mime types.

---

mimetypes( void ) : array

## Parameters

No parameters.

## Return Values

(array) Map of extensions to mime types.

## Examples

```php
foreach (mimetypes() as $mimetype) {
    //
}
```
