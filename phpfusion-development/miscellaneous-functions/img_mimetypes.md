# img_mimetypes()

Array of image mime types.

---

img_mimetypes( void ) : array

## Parameters

No parameters.

## Return Values

(array)

## Examples

```php
foreach (img_mimetypes() as $mimetype) {
    //
}
```
