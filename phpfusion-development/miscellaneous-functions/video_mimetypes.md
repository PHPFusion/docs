# video_mimetypes()

Array of video mime types.

---

video_mimetypes( void ) : array

## Parameters

No parameters.

## Return Values

(array)

## Examples

```php
foreach (video_mimetypes() as $mimetype) {
    //
}
```
