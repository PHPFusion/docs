# is_blacklisted()

Check if user's full or partial ip is blacklisted.

---

is_blacklisted( void ) : bool

## Parameters

No parameters.

## Return Values

(bool)

## Examples

```php
if (is_blacklisted()) {
    //
}
```
